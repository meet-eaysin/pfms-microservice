# Backup directory for Kong configurations

Generated by: npm run dump

This directory stores backup dumps of Kong configuration.
Backups are timestamped and can be used for rollback if needed.

## Usage

Create a backup:

```bash
npm run dump
```

Restore from backup:

```bash
deck sync --config configs/backup/kong-YYYYMMDD-HHMMSS.yaml
```
