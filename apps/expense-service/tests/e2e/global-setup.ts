/**
 * Global Setup for E2E Tests
 * Runs once before all e2e tests
 */

export default async function globalSetup() {
  // Setup test environment, start servers, etc.
  console.log('E2E tests setup complete');
}
