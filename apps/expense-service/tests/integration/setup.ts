/**
 * Integration Test Setup for Expense Service
 * Runs before each test file
 */

beforeAll(async () => {
  // Setup test database connection, etc.
});

afterAll(async () => {
  // Cleanup after tests
});
