{
  "name": "@pfms/config",
  "version": "1.0.0",
  "description": "Shared configuration for PFMS services",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "check-types": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "winston": "^3.11.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@pfms/typescript-config": "workspace:*",
    "@types/dotenv": "^8.2.0",
    "@types/node": "^20.10.6",
    "typescript": "^5.5.2"
  }
}
