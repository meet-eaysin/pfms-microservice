{
  "name": "@pfms/http",
  "version": "1.0.0",
  "description": "HTTP communication utilities with client, circuit breaker, retry logic, and status codes",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@pfms/config": "workspace:*",
    "axios": "^1.6.5"
  },
  "devDependencies": {
    "@pfms/eslint-config": "workspace:*",
    "@pfms/typescript-config": "workspace:*",
    "@types/node": "^20.10.6",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}
